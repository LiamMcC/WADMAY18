extends layout
block content
    .row
      .holder
        h1 Hi Im the reviews page!
         .grid-container
           each review in reviews
            .item
             h1.itemtitle #{review.name}
             p.itemp #{review.content}